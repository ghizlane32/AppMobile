import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  rightItem: {
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
    paddingHorizontal: 10,
  },
  card: {
    paddingHorizontal: 15,
  },
  titleStyle: {},
  leftItem: {padding: 0, marginRight: 0},
});
export default styles;
